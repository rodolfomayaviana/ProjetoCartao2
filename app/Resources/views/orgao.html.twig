{% extends 'base.html.twig' %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" type="text/css" href="{{ asset ('CSS/orgaoCSS.css') }}">
{% endblock %}


{% block body %}
{{ parent() }}

<section id="principal_orgao">
<h3>Pesquisa por Orgao<h3>

<table id="tabela_orgao">
	<tr>
		<th>Orgao Superior</th>
		<th>Orgao Subordinado</th>
		<th>Unidade Gestora</th>
		<th>Ano Transacao</th>
                <th>Mes Transacao</th>
                <th>Nome Transacao</th>
                <th>Portador</th>
                <th>Favorecido</th>
                <th>Data Transacao</th>
                <th>Valor Transacao</th>
	</tr>
	{{ tabela|raw }}
</table>

{% set fatorPaginacao = ( atual // 10) * 10 %}

{% if atual - 10 < 1 %}
        <a href="{{path('orgao', {'page': 1})}}"><<</a>
{% else %}
        <a href="{{path('orgao', {'page': atual - 10})}}"><<</a>
{% endif %}

{% for i in fatorPaginacao .. fatorPaginacao + 9  %}
        {% if i <= paginas %}
                <a href="{{path('orgao', {'page': i + 1 })}}">{{ i + 1 }}</a>
        {% endif %}
{% endfor %}

<a href="{{path('orgao', {'page': fatorPaginacao + 11})}}">>></a>

</section>

{% endblock %}

